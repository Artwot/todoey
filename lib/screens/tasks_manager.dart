import 'package:flutter/material.dart';

class TasksManager extends StatelessWidget {
  const TasksManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
